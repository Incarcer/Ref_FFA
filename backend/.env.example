# Application Security
# Generate a strong secret key, e.g., using: openssl rand -hex 32
SECRET_KEY="<your_super_strong_secret_key_here>"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=10080 # 7 days

# Yahoo API Credentials
YAHOO_CLIENT_ID="<your_yahoo_client_id>"
YAHOO_CLIENT_SECRET="<your_yahoo_client_secret>"
# This must match the redirect URI configured in your Yahoo developer app settings
# For production behind the Nginx proxy, this should be: http://localhost/api/v1/yahoo/callback
YAHOO_REDIRECT_URI="http://localhost:8000/api/v1/yahoo/callback" 

# Frontend URL for redirects
FRONTEND_URL="http://localhost:5173"

# Database URL (for local dev without Docker, will be overridden by docker-compose)
DATABASE_URL="postgresql://admin:supersecret@localhost/fantasy_app"